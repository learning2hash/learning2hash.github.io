---
layout:
title:
---
[
{% for publication in site.publications %}
{
  "key": "{{ publication.path | replace_first: '_publications/', '' | replace: '.markdown', '' }}",
  "citations": "{{ publication.citations }}",
  "year": "{{ publication.year }}",
  "title": {{ publication.title | jsonify }},
  "abstract": {{ publication.content | jsonify }},
  "tags": {{ publication.tags | jsonify }},
  "authors": {{ publication.authors | jsonify }}
}
{% if forloop.rindex0 > 0 %},{% endif %}
{% endfor %}
]
